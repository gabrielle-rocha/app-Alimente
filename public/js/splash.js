document.addEventListener('DOMContentLoaded', function () {
    setTimeout(function () {
        // Redireciona para outra página (por exemplo, a página principal)
        window.location.href = '/exemplo'; 
    }, 3000); // 3000 milissegundos = 3 segundos
});